  <% if @articles.empty? %>
    No current articles
    <% else %>
<table>
  <thead>
    <tr>
      <th>ID</th> 
      <th>Name</th> 
      <th>Body</th> 
      <th>Edit</th> 
      <th>Read</th> 
      <th>Link</th> 
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.id %> </td>
        <td><%= article.name %> </td>
        <td><%= article.body %> </td>
        <td><%= link_to 'Edit article', edit_article_path(article) %> </td>
        <td><%= link_to 'Read article', show_article_path(article) %> </td>
        <td><%= link_to article.to_s, show_article_path(article) %> </td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<%= link_to 'Create new article?', new_article_path %>